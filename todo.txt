
New: Music
    - read directories
    - all id3 data
    - playlists
    - player
	- streaming
	
new: Dashboard
	- weather
		- http://api.openweathermap.org/data/2.5/weather?lat=51.124&lon=5.005&units=metric 
		of http://api.openweathermap.org/data/2.5/weather?q=Geel,be&units=metric&lang=nl
		of http://api.openweathermap.org/data/2.5/forecast?q=Geel,be&units=metric 
		/of http://api.openweathermap.org/data/2.5/find?q=Geel,be&units=metric 
		( http://99webtools.com/blog/get-weather-information-using-php/ )
		- different bakground color per weather type (e.g. sunny = light blue, rainy = grey, clouded = light grey, snow = white, night = dark blue) (css transition)
		- current weather
		- forecast (evening, night, tomorrow)
		- cards?
		- http://www.alessioatzeni.com/meteocons/ - https://github.com/erikflowers/weather-icons
	- traffic overview
		- kaart
		- reistijden
		- reistijd trend vandaag
		- camerabeeldjes?
	- server stats
		- for all servers / hosts / ...
		- serverinfo.sh to start
	- network stats
		- from openwrt
	
Grabber:
    - management
        - counters?
        - schedules
	- cartoons
		- dilbert
		- garfield?
		- sinfest
		- ...
	
Users:
	- action=error => move err messages to switch statement with codes instead of url variable
	- list users
	- show login trials
	- keep logged in

Router:
	
Apps:
	- make settings manageable
	\\- support external apps (transmission / adminer, in iframe?)
	- sub menu's
	- manage actions (set titles)
	- functions for lockfiles => ps aux | grep __filename__ ?

filerep:
	- up/download only
	- flag dirs to reindex => find -ctime -5
	- setting index_building => timestamp, check for timeout
		- or better, check for running processes instead?
	- show conflictfiles (<= already available)
	- setfileindex: split into pre-, per 100 files upload, post-
	
GPS:
	- add multi user support
	- manage alerts for users/places
	- show tracks + replay
	- when entering/leaving place: additional scripts executed
	- when entering/leaving place: check distance difference, if too high, no alert (for leaving)
	?- places: add bigger region for less accurate logging (accuracy is higher)
	- places: distance offset for more/less accurate tracking
		=> only update alerting when at least x time there / x distance covered before
	
Feeds:
	- multi user : auto detect user?
	
	var wshshell=new ActiveXObject("wscript.shell");
	var username=wshshell.ExpandEnvironmentStrings("%username%");
	
	or separate cookie with user id?
	
Messages:
	- add "test send" form
	- manage track&traces
	- manage email alerts
	- message senders (pibot, backdoorcam) : trigger.php
		- in table
		- each it's own properties in table (enabled, priority, ...)
		- silent hours?
		- rate limitation (check t_log_message)
	- show current emails in inbox: subject, from => generate alerting
	- check emails for tracking code?
	
Cameras:
	- view video
	- delete files
	- check status
		http://www.lavrsen.dk/foswiki/bin/view/Motion/MotionHttpAPI
	- toggle motion on/off
	
global:
	- when something happening / done / finished: additional scripts executed
	?- combine crons in new project
	- cleanup sql usage imports after x => combine?
	- cleanup of historical logging
		- messages
		- ...
		- per project a file dbcleanup.php
	
	

optimize table t_;

#update t_directory set date_last_checked = null


#8750
select count(*) from t_directory;
#4010
select count(*) from t_directory where active = 1;
#0
select count(*) from t_directory where date_last_checked is null;
#0
select count(*) from t_directory where active = 1 and date_last_checked is null;
#	2015-04-09 08:01:03		2015-04-09 21:28:29		=>	13:27:26  of  132726
#   2015-04-16 14:07:04     2015-04-19 00:01:07     =>  2859403
select min(date_last_checked), max(date_last_checked),max(date_last_checked) - min(date_last_checked)  from t_directory;

#281364
select count(*) from t_file;
#281363
select count(*) from t_file where active = 1;
